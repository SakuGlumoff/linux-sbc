name: Build

on:
  push:
    branches:
      - trunk
    pull_request:

jobs:
  build:
    runs-on: [self-hosted, docker]
    container:
      image: kicad/kicad:9.0
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          submodules: true
      - name: Install fonts
        run: |
              sudo chmod 644 fonts/courier_new.ttf
              sudo mv fonts/courier_new.ttf /usr/local/share/fonts
              fc-cache
      - name: Generate PDF
        run: kicad-cli sch export pdf --output linux-sbc.pdf --default-font "Courier New" linux-sbc.kicad_sch
      - name: Archive schematics
        uses: actions/upload-artifact@v4
        with:
          name: schematics
          path: linux-sbc.pdf
          if-no-files-found: error
